# Local private configuration

Place hardware-calibrated maps, policy paths, sensor addresses, and robot
parameters here. Files in this directory are ignored by Git.

Do not commit real hostnames, usernames, IP addresses, device identifiers,
policy weights, or experiment-specific tuning.
