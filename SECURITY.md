# Security and responsible deployment

Do not place credentials, robot addresses, device identifiers, maps, policy
weights, recorded data, or calibrated parameters in public issues or commits.

The included command bridge starts disabled and uses conservative public
limits. Physical deployment still requires independent safety review,
watchdogs, emergency-stop hardware, and platform-specific validation.
